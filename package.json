{
  "name": "bible-social",
  "version": "1.0.0",
  "description": "Faith Buddies - Firebase-powered social backend",
  "type": "module",
  "scripts": {
    "serve": "firebase emulators:start",
    "serve:functions": "firebase emulators:start --only functions",
    "serve:firestore": "firebase emulators:start --only firestore",
    "serve:storage": "firebase emulators:start --only storage",
    "deploy": "firebase deploy",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:firestore": "firebase deploy --only firestore",
    "deploy:storage": "firebase deploy --only storage"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^4.22.2",
    "firebase-admin": "^14.2.0"
  }
}
