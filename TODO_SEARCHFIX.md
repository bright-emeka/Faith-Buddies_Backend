# TODO - Search fix

- [x] Confirm searchUsers returns `response.data` but Search component expects `results.users || results || []`.
- [x] Ensure backend returns `{ users: [...] }` OR frontend adapts.


